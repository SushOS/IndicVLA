# OS / editor
.DS_Store
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/
.env
.env.*

# Conda / local env files
.python-version

# Build / packaging
build/
dist/
*.egg-info/

# Model / notebook / cache noise
.cache/
.ipynb_checkpoints/

# Local Hugging Face and model cache overrides if created inside repo
hf_cache/
models/
checkpoints/

# Generated corpus artifacts
corpus_build/*.jsonl
corpus_build/*.json
!corpus_build/.gitkeep


# Run 2/3 large binaries -- these live on HuggingFace (CodeSushh/maila-run2-motionpivot),
# verified by SHA-256 in run2_artifacts/TEARDOWN_MANIFEST.json before the box was destroyed.
OMG/run2_artifacts/**/*.pt
OMG/run2_artifacts/**/*.mp4
OMG/run2_artifacts/**/*.npz
OMG/run2_artifacts/**/*.tgz
OMG/side_by_side.zip
OMG/side_by_side/
